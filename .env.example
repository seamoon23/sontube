DATABASE_URL="file:./dev.db"
SESSION_SECRET="replace-with-a-long-random-local-secret"
ADMIN_PASSWORD_HASH=""

# MVP note:
# Do not add YouTube or Google API keys; this app must not need them.
# Create ADMIN_PASSWORD_HASH with:
# npm run auth:hash -- <admin-password> <same-session-secret>
